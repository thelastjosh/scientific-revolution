import type { Task } from "@shared/network-feed";

export type { Task };

/** Deprecated placeholder kept only to avoid compile breaks while old pages are migrated. */
export const MOCK_TASKS: Task[] = [];
